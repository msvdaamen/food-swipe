{
  "name": "database",
  "type": "module",
  "scripts": {
    "make:migration": "bunx kysely migrate:make ",
    "migrate": "bunx kysely migrate:latest",
    "rollback": "bunx kysely migrate:rollback --all"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "kysely": "^0.27.4",
    "kysely-postgres-js": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "kysely-ctl": "^0.9.0"
  },
  "peerDependencies": {
    "typescript": "^5.5.4"
  }
}
