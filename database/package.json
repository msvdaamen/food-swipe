{
  "name": "database",
  "type": "module",
  "scripts": {
    "make:migration": "npx kysely migrate:make ",
    "migrate": "npx kysely migrate:latest",
    "rollback": "npx kysely migrate:rollback --all"
  },
  "dependencies": {
    "kysely": "^0.27.4",
    "kysely-postgres-js": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "kysely-ctl": "^0.9.0"
  },
  "peerDependencies": {
    "typescript": "^5.5.4"
  }
}
